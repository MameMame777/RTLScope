[package]
name = "rtlscope-ir"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Shared intermediate representation for RTLScope. Type definitions only, no analysis logic."

[dependencies]
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
